const skinCache="skin-static";let regex=/(js|css|svg|webp|png|jp(e*)g|woff2|)/;let assets=[];self.addEventListener("install",(evt)=>{evt.waitUntil(caches.open(skinCache).then((cache)=>{cache.addAll(assets);}));});self.addEventListener("activate",(evt)=>{});self.addEventListener("fetch",(evt)=>{evt.request.url.match(regex)&&assets.push(evt.request.url);assets=[...new Set(assets)];evt.respondWith(caches.match(evt.request).then((cacheRes)=>{return cacheRes||fetch(evt.request);}));});