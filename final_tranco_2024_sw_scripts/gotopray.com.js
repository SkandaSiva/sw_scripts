var cacheVersion=4;var currentCache={offline:'offline-cache'+cacheVersion};const offlineUrl='https://gotopray.com/offline.html';self.addEventListener('install',event=>{event.waitUntil(caches.open(currentCache.offline).then(function(cache){return cache.addAll([offlineUrl]);}));});this.addEventListener('fetch',event=>{if(!navigator.onLine){event.respondWith(fetch(event.request.url).catch(error=>{console.log(error);if(!navigator.onLine){console.log('app is offline');return caches.match(offlineUrl);}}));}});