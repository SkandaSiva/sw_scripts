self.addEventListener('fetch',function(event){});self.addEventListener('push',function(event){var data=event.data.json().notification;const promiseChain=self.registration.showNotification(data.title,{body:data.body,icon:data.icon,data:{url:data.click_action}});event.waitUntil(promiseChain);});self.addEventListener('notificationclick',function(event){event.notification.close();if(event.notification.data.url!=null){event.waitUntil(clients.openWindow(event.notification.data.url+'?utm_referrer=https://webpush.coindar.org'));}})