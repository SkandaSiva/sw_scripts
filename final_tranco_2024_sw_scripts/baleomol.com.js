"use strict";self.addEventListener("push",function(event){try{console.log("[Service Worker] Push Received.");console.log(`[Service Worker] Push had this data: "${event.data.text()}"`);const data=event.data.text()&&JSON.parse(event.data.text());const title=data?.title;const options={body:data?.body,icon:`${self.location.origin}/brand/baleomol-192x192.png`,};event.waitUntil(self.registration.showNotification(title,options));}catch(error){console.log("error sw",{error});}});