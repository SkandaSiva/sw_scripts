/* eslint-disable no-undef */
/* eslint-disable import/unambiguous */
/* eslint-disable filenames/match-regex */
/* xtremepush START */
importScripts(
  decodeURIComponent(
    location.search.slice(Math.max(0, location.search.indexOf('ref=') + 4)),
  ),
);
/* xtremepush END */
