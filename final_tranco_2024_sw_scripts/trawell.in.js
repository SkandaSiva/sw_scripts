self.addEventListener('install',event=>{event.waitUntil(caches.open('tw-pwa').then(cache=>cache.addAll(['/home','/tour-packages/','/tour/','/stays/','/css/common_2020.css','/images/trawell_new_social_circle_sm.png'])).then(()=>console.log('content is now available offline')).catch(()=>console.log('oh noes! something went wrong')));});self.addEventListener('activate',event=>{console.log('tw-pwa');});self.addEventListener('fetch',event=>{event.respondWith(caches.match(event.request).then(function(response){return response||fetch(event.request);}));});self.addEventListener('push',function(event){event.waitUntil(self.registration.showNotification('Got Push?',{body:'Push Message received'}));});